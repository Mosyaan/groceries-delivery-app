function addToCart() {
    let button = addToCart = document.querySelector("div.item > button.item__buy");

    button.addEventListener('click', () => {
        localStorage.setItem('')
    })
}

addToCart()
